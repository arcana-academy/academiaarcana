repos:
  - repo: local
    hooks:
      - id: git-diff-check
        name: git diff check
        entry: python -c "import subprocess; raise SystemExit(subprocess.call(['git', 'diff', '--check']))"
        language: python
        pass_filenames: false
