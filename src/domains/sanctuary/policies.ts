export type SanctuaryPolicyContext = Record<string, unknown>;

export const SanctuaryPolicies = {
  // Domain-specific business rules will be implemented here.
  // Authentication and hierarchical access are handled by the application/infrastructure layers.
};
