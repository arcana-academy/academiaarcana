import { describe, expect, it } from "vitest";
import * as adaptiveContracts from "./contracts";

describe("adaptive contracts", () => {
  it("can be imported as a module", () => {
    expect(adaptiveContracts).toBeDefined();
  });
});
